/**
 * Zaya deployment configuration.
 *
 * This file is loaded before the app and is the supported place for per-deployment settings.
 * (Inline <script> blocks in index.html are blocked by the Content-Security-Policy.)
 */

// Default document shown when no ?pdf= parameter and no previously opened PDF exists.
// Remote URLs must be served with CORS enabled. Relative paths are resolved against index.html.
// window.ZAYA_DEFAULT_PDF = "./documents/my-book.pdf";

// Private (Pro) builds set this to true so lib/js/pro-features/index.js is loaded.
// window.ZAYA_PRO = true;
